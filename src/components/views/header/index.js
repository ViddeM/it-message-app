import React from 'react';
import { HeaderContainer, HeaderCard, HeaderText } from './styles';

export const Header = () =>
    <HeaderContainer>
        <HeaderCard>
            <HeaderText>IT Messaging</HeaderText>
        </HeaderCard>
    </HeaderContainer>